{
  "name": "vidquery",
  "private": true,
  "scripts": {
    "setup": "npm install && npm --prefix frontend install && cd backend && uv sync",
    "dev": "concurrently --kill-others --kill-signal SIGKILL \"cd backend && uv run fastapi dev main.py\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "check:backend": "npm run check:backend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
